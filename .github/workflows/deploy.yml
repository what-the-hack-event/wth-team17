name: Deploy

on:
  workflow_dispatch:

jobs:
  Deploy-Resources:
    runs-on: ubuntu-latest

    steps:
      - name: Set up Azure CLI
        uses: azure/login@v1
        with:
          creds: ${{ secrets.AZURE_CREDENTIALS }}

      - name: Install Bicep CLI
        run: |
          curl -Lo bicep https://github.com/Azure/bicep/releases/latest/download/bicep-linux-x64
          chmod +x ./bicep
          sudo mv ./bicep /usr/local/bin/bicep
          bicep --help
        
      - name: Deploy Bicep template
        run: az deployment sub create --location uksouth --template-file ${{ github.workspace }}/InfrastructureAsCode/main.bicep

    #   - name: Deploy Bicep file
    #     uses: azure/arm-deploy@v1
    #     with:
    #       subscriptionId: ${{ secrets.AZURE_SUBSCRIPTION }}
    #       resourceGroupName: ${{ secrets.AZURE_RG }}
    #       template: ./InfrastructureAsCode/main.bicep
    #       parameters: 'storagePrefix=mystore storageSKU=Standard_LRS'
    #       failOnStdErr: false

    #   - name: Deploy Azure Resource Manager
    #     uses: azure/CLI@v1
    #     with:
    #       azcliversion: 2.28.0
    #       inlineScript: |
    #         az deployment sub create --location uksouth --template-file ./InfrastructureAsCode/main.bicep
